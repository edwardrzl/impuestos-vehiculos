JWT_SECRET=

# Clave de la API de Anthropic (Claude). Se usa para validar automáticamente
# con IA las fotos de las tarjetas de propiedad en las solicitudes de traspaso.
# Si se deja vacía, el servidor arranca igual pero el endpoint de traspaso
# responde 503 al intentar crear una solicitud.
ANTHROPIC_API_KEY=
